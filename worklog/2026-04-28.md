# 2026-04-28

## 工作内容
- 调整 [panel/app.js](../panel/app.js) 中充值/套餐记录的派生逻辑，不再读取 `实际额度` 或旧 `倍率`，统一改为运行时按“会员折扣”是否为空推导。
- 当 `会员折扣` 为 `null` 时，改为用 `金额 / 额度` 计算倍率，且金额 tooltip 只显示“额度”。
- 当 `会员折扣` 有值时，改为先计算 `实际额度 = 额度 / 会员折扣`，再用 `金额 / 实际额度` 计算倍率，且金额 tooltip 显示“额度 + 会员折扣 + 实际额度”。
- 将充值/套餐数据从根目录聚合文件拆分到各站点目录：`site/<站点名>/topup+plans.json`，每个文件顶层改为单站点数组。
- 统一拆分后的充值/套餐记录格式，每条只保留 `类型`、`套餐名`、`金额`、`额度`、`会员折扣` 五个字段，并移除根目录 `site/topup+plans.json`。
- 修改 [panel/app.js](../panel/app.js) 的数据加载逻辑，改为遍历站点目录时分别读取 `<站点>.json` 和同目录的 `topup+plans.json` 后再组合。
- 修改 [main.py](../main.py) 的提示与校验逻辑，改为针对本轮成功站点逐个创建/校验 `site/<站点名>/topup+plans.json`，且要求顶层为数组。
- 更新 [README.md](../README.md) 中充值/套餐数据的路径、示例结构和维护说明。
- 新增最小回归测试 [tests/test_topup_plans_logic.js](../tests/test_topup_plans_logic.js)，覆盖“会员折扣为空/非空”两种场景下的额度、实际额度与倍率推导。

## 验证
- 运行 `node --check panel/app.js`，确认前端脚本语法通过。
- 运行 `python -m pytest tests/test_main_panel_flow.py -q`，结果为 `7 passed`。
- 运行 `node tests/test_topup_plans_logic.js`，确认推导逻辑与逐站点读取路径断言通过。
- 运行迁移校验脚本，确认已生成 18 个 `site/<站点名>/topup+plans.json`，全部顶层为数组，且每条记录字段集合等于 `类型/套餐名/金额/额度/会员折扣`。

## 简短复盘
- 值得保留：把 `topup+plans.json` 限制为原始业务字段，并按站点拆到各自目录，既减少人工维护时的数据漂移，也让充值/套餐数据与站点模型数据的归属保持一致。
- 可优化点：当前前端读取与推导逻辑仍主要靠最小 Node 断言覆盖；后续若继续调整面板，最好补一个真实页面层的自动化验收，直接校验 tooltip 文案和多站点组合展示。
